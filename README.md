Simple script to install docker compose on debian

Invoke with 
```curl -fsSL https://raw.githubusercontent.com/beaufromit/docker-compose-installer/refs/heads/master/install-docker-compose.sh | bash```
